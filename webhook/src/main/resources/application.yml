server:
  port: 8081
spring:
  application:
    name: "webhook"
  datasource:
    url: jdbc:mysql://localhost:3306/zapier
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
